{
  "name": "myblog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"drewst.one\" >> out/CNAME && gh-pages -d out --branch master"
  },
  "dependencies": {
    "@mdx-js/loader": "^1.5.5",
    "@next/mdx": "^9.2.1",
    "isomorphic-unfetch": "^3.0.0",
    "next": "9.2.1",
    "next-seo": "^4.1.0",
    "react": "16.12.0",
    "react-dom": "16.12.0",
    "react-ga": "^2.7.0",
    "style-loader": "^1.1.3"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "rifraf": "^2.0.3"
  }
}
